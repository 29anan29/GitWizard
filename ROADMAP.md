# GitWizard 路线图（Roadmap）

**版本**：v1.0
**状态**：已确认（2026-08-26）
**关联文档**：`PRD.md`（需求）、`DESIGN.md`（视觉规范）
**技术基线**：Tauri v2 (Rust) + SvelteKit + TypeScript + git2-rs 静态链接 + 中英双语 i18n

---

## 0. 总览

| 阶段 | 目标 | 时间线 | 出口标准 |
| :--- | :--- | :--- | :--- |
| **P0 - MVP** | 跑通「提交代码」完整闭环 | 第 4 周 | Windows `.msi` + Linux 包可安装，向导走通 提交→推送 |
| **P1 - 核心功能补齐** | 覆盖日常高频操作 | 第 8 周 | 拉取/分支/合并可用，三平台 CI 出包 |
| **P2 - 体验打磨** | 安全网 + 内测 | 第 12 周 | 撤销栈上线，小规模内测反馈收集 |
| **P3 - 正式版 v1.0** | 发布 | 第 16 周 | 官网/开源发布，KPI 达标 |

依赖关系：P0 的通用五步契约骨架是所有后续工作流的复用基础；安全网（撤销栈）在 P0 已埋数据埋点（HEAD 备份点），P2 完成交互。

---

## P0 - MVP（第 1–4 周）：脚手架 + 提交工作流

### M0.1 项目脚手架（第 1 周上半）
- [ ] `pnpm create tauri-app`：SvelteKit + TypeScript 模板，目录结构见规划（`src/` 前端、`src-tauri/` 后端）
- [ ] 空窗口跑通：`pnpm tauri dev` 正常启动
- [ ] 工具链就绪：`svelte-check`、`cargo clippy`、`cargo test` 接入

### M0.2 设计系统落地（第 1 周下半）
- [ ] `tokens.css`：DESIGN.md 全套令牌
  - 色板：背景 `#f2f1ed`、文字 `#26251e`、强调 `#f54e00`、错误 `#cf2d56`、成功 `#1f8a65`、Surface 100–500
  - 边框三级：oklab 暖棕 `rgba(38,37,30, .1/.2/.55)` 回退写法
  - 字阶与负字距规则（72px/-2.16px → 36px/-0.72px → 26px/-0.325px）、8px 间距体系、圆角标尺、阴影层级
- [ ] 基础组件：Button（主按钮 / 胶囊 / 幽灵）、Card、Input、步骤进度条
- [ ] 图标方案：Iconify SVG（curl 拉取本地化），**禁用 emoji**
- [ ] 决策记录：CursorGothic/jjannon/berkeleyMono 为商业字体，MVP 使用 DESIGN.md 指定的系统回退栈，后续视授权替换

### M0.3 Rust Git 引擎（第 2 周）
- [ ] 依赖集成：`git2`（静态 libgit2，不依赖系统 Git）、`tokio`、`serde`、`confy`、`rfd`
- [ ] Commands：
  - `open_repo`：文件夹选择 + 仓库校验
  - `get_status`：变更文件按 新增/修改/删除 分组
  - `stage_files` / `unstage_files`：暂存操作与统计
  - `commit`：创建提交（作者信息读取全局配置或应用内设置）
  - `push`：HTTPS 推送，凭据回调
  - `get_recent_repos` / `add_recent_repo`：confy 持久化
- [ ] 所有命令经 `tokio::spawn` 异步执行，不阻塞 UI
- [ ] 日志通道：等价 git 命令及输出通过 Tauri Event 实时推送前端
- [ ] 安全埋点：任何写操作前自动记录 HEAD 备份点（为 P2 撤销栈供数）
- [ ] 单元测试：临时目录构建测试仓库覆盖 status/stage/commit/push(mock remote)

### M0.4 启动面板（第 3 周上半）
- [ ] 顶部仓库选择器（打开文件夹 / 最近仓库列表）
- [ ] 6 张情景入口卡片（📤 提交 / 📥 拉取 / 🌿 分支 / 🔀 合并 / ⏱️ 回退 / ⚡ 快捷指令）
  - 「提交」卡可用，携带待提交文件数红点徽标
  - 其余 5 张渲染但置灰，标注「即将上线」
- [ ] 底部全局控制台抽屉：默认折叠，展开显示实时命令回显（面向老手的信任机制）

### M0.5 提交工作流向导（第 3 周下半 – 第 4 周）★ 核心
- [ ] 通用五步契约骨架（所有未来工作流复用）：
  前置检查 → 参数收集 → 影响预览 → 执行与进度 → 结果反馈
- [ ] 顶部导航条：「步骤 x/4」+ 上一步 + 取消
- [ ] Step 1 选择变更：文件分组勾选（全选/按组选）、底部暂存区统计
- [ ] Step 2 撰写信息：模板快捷片（`feat:` `fix:` `docs:` `chore:`）+ 基于文件名的规则式摘要生成（v1.0 不接模型）
- [ ] Step 3 执行策略：☑️ 提交后立即推送
- [ ] Step 4 执行与结果：进度展示 + 成功绿页 / 失败红页
  - 失败若为非快进（远端有新提交），引导至「拉取」流程占位提示
- [ ] i18n：自研轻量 store（zh-CN 默认 / en 字典 JSON），全部文案走字典

### M0.6 打包与验证（第 4 周末）
- [ ] 本机出 Linux `.deb` / `.AppImage`
- [ ] GitHub Actions CI：Windows `.msi`（未签名）/ macOS `.dmg`（提前打通流水线）
- [ ] 体积校验 < 50MB
- [ ] 手动冒烟清单：真实测试仓库完整走通「打开仓库→勾选→填写信息→提交→推送」
- [ ] **P0 验收**：从未接触 Git 的用户 5 分钟内完成首次推送（PRD KPI）

---

## P1 - 核心功能补齐（第 5–8 周）

- [ ] **拉取/变基工作流**：Fetch + Pull(`--rebase` 可选)，冲突时引导进入解决视图；承接 P0 结果页的失败闭环
- [ ] **分支管理工作流**：新建/切换/重命名/删除，基于当前分支状态的合法性前置检查
- [ ] **合并工作流**：策略选择（默认 `--no-ff`）；基础冲突解决——左右 Diff 对比视图、「保留我的/他们的」快捷按钮、内置简易编辑器；全部解决后自动 `add` + 合并提交
- [ ] **凭证升级**：接入系统原生凭据管理器（Windows Credential Manager / macOS Keychain / Linux libsecret），替代 MVP 内存方案
- [ ] **SSH 基础支持**：默认读取 `~/.ssh` 密钥（多密钥管理留 P3）
- [ ] **CI 补齐**：三平台产物矩阵（msi/dmg/deb/AppImage），tag 自动发 Release
- [ ] 启动面板 5 张置灰卡片中「拉取」「分支」「合并」点亮

## P2 - 体验打磨与预发布（第 9–12 周）

- [ ] **全局撤销栈（安全网）**：侧边栏「撤销上一步」，简单场景自动执行（如 `reset --soft HEAD~1`），复杂场景给恢复指引；消费 P0 埋的 HEAD 备份点
- [ ] **危险操作双确认强化**：`reset --hard` / `push --force` 长按 2 秒确认组件
- [ ] **断点续作**：崩溃后重启检测未完成工作流状态，提示继续或放弃
- [ ] **深色主题**：DESIGN.md 暖色令牌的暗色映射
- [ ] **快捷键支持**：全局与向导内快捷键
- [ ] **历史回退工作流**：Reset / Revert 向导化
- [ ] 小规模内测：发放安装包，收集 KPI 数据（上手时间、点击次数、崩溃率）

## P3 - 正式版 v1.0（第 13–16 周）

- [ ] SSH 多密钥管理 UI
- [ ] 历史提交可视化（简单视图：提交图 + 检索）
- [ ] 「快捷指令」卡片上线：自定义组合流程（高阶功能）
- [ ] 性能达标复核：仓库初始化 <200ms、10 万文件状态刷新 <500ms、启动 <1s
- [ ] 正式开源 / 官网发布 / 三平台正式渠道分发
- [ ] Crash Rate < 0.5% 监控看板

---

## 附 A：关键技术决策记录（ADR）

| # | 决策 | 理由 |
| :- | :--- | :--- |
| 1 | 前端选 SvelteKit | PRD 首选；编译时优化无 VDOM 开销，适合步骤向导逻辑，包体更小 |
| 2 | Git 交互纯 git2-rs 静态链接 | PRD 约束「双击即用」，不调用系统 Git 环境 |
| 3 | MVP 凭据仅存内存 | OS 钥匙串集成移至 P1，降低 P0 复杂度 |
| 4 | 商业字体先用系统回退栈 | CursorGothic/jjannon/berkeleyMono 未授权，观感差异后续迭代 |
| 5 | Windows MSI 由 CI 产出 | 开发机为 Linux |
| 6 | AI 摘要 v1.0 为规则拼接 | PRD 明示先做基于文件名的规则方案，模型接入后置 |
| 7 | i18n 自研轻量 store | 两份 JSON 字典零依赖，避免额外框架风险 |

## 附 B：风险与对策

| 风险 | 影响 | 对策 |
| :--- | :--- | :--- |
| git2-rs 推送凭据回调在 Linux 各桌面环境行为不一 | P0 push 失败率上升 | 凭据输入框兜底；P1 接 libsecret |
| libgit2 对部分 Git 特性支持滞后 | 个别仓库兼容性问题 | 维护不支持特性清单并在 UI 明示 |
| 冲突 Diff 视图复杂度超预期 | P1 延期 | 先做行级二选一，编辑器合并放 P2 打磨 |
| 安装包超 50MB | 违背 NFR | 检查静态链接冗余、前端资源压缩、按平台裁剪图标资源 |
