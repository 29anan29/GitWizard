# 产品需求文档（PRD）
## 产品名称：GitWizard（项目代号）
**版本**：v1.0.0
**文档状态**：草稿
**目标平台**：Windows 10+ / macOS 11+ / Linux (Ubuntu 20.04+)

---

## 1. 产品定位与核心理念

### 1.1 产品定位
**"Git 的自动化流程控制台"**。
GitWizard 不是另一个功能堆叠的 Git GUI，而是一个**意图驱动（Intent-Driven）** 的工具。它将底层的 Git 命令行操作封装为一系列“场景化工作流”，用户通过跟随步骤向导（类似软件安装向导 “Next -> Next -> Finish”）即可完成复杂的版本控制任务。

### 1.2 核心理念
- **CLI 彻底隐形**：用户操作的是“任务”，而非“命令”。Git 命令作为后台引擎存在，仅在日志区可选可见。
- **流程即界面（Flow as Interface）**：界面布局随当前操作动态变化，只展示当前步骤必要的信息，杜绝信息过载。
- **单进程合体**：最终交付物为单一可执行文件或标准安装包，无独立运行时依赖（无需预装 Git、Node.js 或 Web 服务器）。

---

## 2. 目标用户画像

| 画像 | 特征 | 核心痛点 |
| :--- | :--- | :--- |
| **初级开发者** | 了解 Git 基本概念，但不熟悉命令行参数 | 一遇冲突就紧张；不敢用 `rebase`；`reset` 后丢代码。 |
| **泛技术角色** | 产品、设计、测试，偶尔需提交文档或查看版本 | 只想把文件传上去，不想记 `add/commit/push` 的先后顺序。 |
| **效率型开发者** | 熟练 CLI，但希望减少重复机械输入 | 懒于每次都写长路径；希望一键完成“暂存+提交+推送”标准动作。 |

---

## 3. 产品核心功能详细设计

### 3.1 功能模块总览
系统由 **“启动面板”** 和 **“工作流引擎”** 两大部分组成。

#### 3.1.1 启动面板（Home Dashboard）
- **功能描述**：应用启动后的主界面，不是仓库文件列表，而是一组**情景化入口卡片**。
- **卡片列表**：
  1. **📤 提交我的修改**（标准 Commit + Push）
  2. **📥 拉取最新代码**（Fetch + Pull/Rebase）
  3. **🌿 新建/切换分支**（Create / Checkout）
  4. **🔀 合并分支**（Merge）
  5. **⏱️ 历史回退**（Reset / Revert）
  6. **⚡ 快捷指令**（自定义组合流程，高阶功能）
- **交互细节**：每个卡片携带当前仓库状态小红点（如“2 个待提交文件”），点击即进入对应流程。

#### 3.1.2 工作流核心引擎（Workflow Engine）
这是 PRD 的核心，定义了所有操作必须遵循的“步骤契约”。

**通用步骤契约**：
1. **前置检查（Pre-check）**：自动检测仓库状态是否符合启动条件（如合并前需先提交本地修改）。
2. **参数收集（Input）**：通过表单收集必要信息。
3. **影响预览（Preview）**：显示即将发生的变更图解（如文件数量、提交哈希变化），并提供**“显示对应 Git 命令”**的折叠按钮。
4. **执行与进度（Execute）**：点击“执行”后，显示实时进度条和后台命令回显。
5. **结果反馈（Result）**：成功/失败弹窗，失败时提供**一键重试**或**回滚**建议。

---

### 3.2 典型工作流详述（User Stories）

#### Story 1：提交代码（Commit & Push）
1. **入口**：点击启动面板的“提交我的修改”。
2. **Step 1 - 选择变更**：
   - UI 展示所有变更文件，按“新增/修改/删除”分组。
   - 交互：勾选要提交的文件（支持全选/按分组选）。
   - 底部显示“暂存区大小”。
3. **Step 2 - 撰写信息**：
   - 输入框支持**常用模板快捷填入**（如 `feat: `, `fix: `）。
   - 针对多文件变更，**AI 自动摘要生成**（调用本地轻量模型或 Git 钩子，v1.0 可先做基于文件名的规则拼接）。
4. **Step 3 - 执行策略**：
   - 选项：☑️ 提交后立即推送到远端；☑️ 提交前运行代码格式化/测试（可配置忽略）。
   - 点击 **“提交”**。
5. **Step 4 - 结果**：绿色成功提示或红色报错。报错若为“远端有新提交”，**自动弹出“拉取”工作流入口**，形成闭环。

#### Story 2：合并分支（Merge）
1. **Step 1 - 选择源分支**：下拉框选择要将当前分支合并入的目标分支（或反向）。
2. **Step 2 - 合并策略**：单选按钮（默认 `--no-ff` / `--ff-only`）。
3. **Step 3 - 冲突预览**：
   - 若检测到冲突，**界面立即分裂为左右对比视图（Diff View）**。
   - 提供按钮：✅ 保留我的 / ✅ 保留他们的 / ✏️ 手动编辑（调用系统默认编辑器或内置编辑器）。
   - 所有冲突文件解决后，自动执行 `git add` 并完成合并提交。
4. **Step 4 - 完成**：显示合并后的提交图高亮。

---

### 3.3 异常处理与“安全网”设计
- **危险操作双确认**：针对 `Reset --hard` 或 `Push --force`，除确认勾选框外，必须**长按确认按钮 2 秒**才生效。
- **本地回滚栈**：在执行任何破坏性操作前，自动备份当前 `HEAD` 指向，并在侧边栏提供一个 **“撤销上一步流程”** 按钮（针对简单提交，执行 `git reset --soft HEAD~1`；针对复杂操作提供指引）。
- **断点续作**：如果应用崩溃，重启后自动检测未完成的工作流状态，提示用户继续或放弃。

---

## 4. 非功能需求（NFR）

| 类别 | 具体要求 |
| :--- | :--- |
| **性能** | 仓库初始化（< 200ms）；含 10 万文件的仓库文件状态刷新（< 500ms）；启动耗时（< 1s）。 |
| **兼容性** | 支持 Git 协议 v2；兼容 GitHub/GitLab/自建 Gerrit；支持 SSH 私钥（默认读取 `~/.ssh`）。 |
| **体积** | 安装包大小控制在 **50MB 以内**（Tauri 基础优势）。 |
| **安全** | 凭证管理：优先使用系统原生凭据管理器（Windows Credential Manager / macOS Keychain），不落盘存储明文密码。 |

---

## 5. 技术架构与实现约束

### 5.1 技术选型
- **前端框架**：**SvelteKit** (推荐) 或 Vue 3 + TypeScript。
  - *理由*：Svelte 响应式极简，适合编写复杂的步骤逻辑；无虚拟 DOM 开销。
- **后端核心**：**Rust (Tauri v2)**。
  - *桌面后端与前端通信*：Tauri 的 `invoke` + **Events**（用于后端主动推送 Git 实时日志）。
- **Git 交互库**：**`git2-rs`** (libgit2 绑定)。
  - *约束*：**不调用系统 Git 环境**，完全静态链接 libgit2，确保“双击即用”。
- **异步运行时**：`tokio` + `async-trait`，确保大仓库操作不阻塞 UI。

### 5.2 前后端合体构建策略
- **资源嵌入**：前端 `dist` 目录通过 `tauri-build` 的 `include_resources` 编译进二进制文件的 `$PATH` 中。
- **安装包产出**：
  ```toml
  # tauri.conf.json 配置摘要
  {
    "bundle": {
      "active": true,
      "icon": ["icons/32x32.png", ...],
      "targets": ["msi", "dmg", "deb", "appimage"],
      "windows": {
        "certificateDigest": null, // 允许未签名构建，后期可加
        "wix": { /* 自定义安装路径 */ }
      }
    }
  }
  ```

### 5.3 数据持久化（User Preferences）
- 使用 Rust 的 `serde` + `confy` 库，将用户配置（如默认提交模板、忽略的文件后缀）存储于系统标准配置目录：
  - Windows: `%APPDATA%\GitWizard\config.toml`
  - macOS: `~/Library/Application Support/GitWizard/config.toml`

---

## 6. 用户界面设计原则（UI/UX）

- **向导导航条**：所有流程顶部都有进度条（步骤 1/4），并提供明确的 **“上一步”** 和 **“取消”** 按钮。
- **状态区分**：使用高亮色彩（绿/红/橙）强烈区分当前步骤是“安全（确认）”、“风险（危险）”还是“等待（输入）”。
- **全局命令展示区**：在界面底部设立一个 **可折叠的控制台（Console）**。
  - 默认折叠（面向新手），展开后显示后台执行的精确 `git` 命令及其标准输出。
  - *目的*：既帮助新手屏蔽复杂性，又帮助老手信任软件的可靠性（所见即所得）。
- 整体DESIGN看DESIGN.md


---

## 7. 里程碑与发布规划

| 阶段 | 时间线 | 交付物 |
| :--- | :--- | :--- |
| **P0 - MVP (最小可行产品)** | 第 4 周 | 1. 项目脚手架搭建；2. 完成“提交”工作流（Add -> Commit -> Push）；3. 支持 Windows `.msi` 包生成。 |
| **P1 - 核心功能补齐** | 第 8 周 | 1. 加入“拉取/变基”、“新建分支”、“合并（含基础冲突解决）”流程；2. 打包 macOS `.dmg` 和 Linux `.deb`。 |
| **P2 - 体验打磨与预发布** | 第 12 周 | 1. 全局撤销栈（安全网）；2. 深色主题；3. 快捷键支持；4. 开放小规模内测。 |
| **P3 - 正式版 v1.0** | 第 16 周 | 1. 支持 SSH 多密钥管理；2. 增加“历史提交可视化简单视图”；3. 正式开源/发布官网。 |

---

## 8. 成功指标（KPI）
- **新手上手时间**：从未接触过 Git 的用户，在无指导情况下，通过软件在 **5 分钟内** 完成首次推送。
- **命令调用缩减**：完成一次标准合并流程，用户点击次数不超过 **6 次**。
- **崩溃率**：正式版 Crash Rate < 0.5%。

---
